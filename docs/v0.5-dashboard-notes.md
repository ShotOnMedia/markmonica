# v0.5 dashboard implementation notes

The first dashboard pass is intentionally presentation-only and uses the existing event query. This keeps the initial change low risk while establishing the new host product layout.

The next dashboard increment will add aggregated media statistics in the backend and expose them to these event cards. That change should be implemented separately and tested against real event/media rows before deployment.